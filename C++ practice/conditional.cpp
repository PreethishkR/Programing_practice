#include<iostream>
using namespace std;

int main(){
    /*int a,b;
    cout <<"enter a & b \n";
    cin>>a>>b;
    if(a%2==0){
        cout<<a<<" is even \n";
    }
    else{
        cout<<a <<" is odd"<<endl;

    }
    if(b%2==0){
        cout<<b<<" is even \n";
    }
    else{
        cout<<b<<" is odd \n";
    }
    */

   //income
   /*
   int income;
   float tax;
   cout<<"enter the income (in lakh)";
   cin>>income;
   if(income <5){
    tax=0;
   }
   else if(income<=10){
    tax=0.20*income;
   }
   else{
    tax=0.30*income;
   }
    cout<<"tax="<<(tax*100000)<<endl;
    */

   //largest of 3 numbers
   /*int a,b,c;
   cout<<"enter a,b,c";
   cin>>a>>b>>c;
   if(a>=b && a>=c){
    cout<<"largest number is:"<<a<<endl;
   }
   else if(b>=c){
    cout<<"largest number is:"<<b<<endl;
   }
   else 
    cout<<"largest number is:"<<c<<endl;
    */

   //ternary operator
  /* int isadult;
   string even_odd;
   int a,b;
   cout<<"enter 2 numbers";
   cin>>a>>b;
   isadult=a>b? a:b;
   even_odd=a%2==0? "even":"odd";
   cout<<isadult<<endl;
    cout<<even_odd<<endl;
    */

   //switch 
   /*
   
   int day;
   cout<<"enter day:";
   cin>>day;

   switch (day)
   {
   case 1: cout<<"moday \n";
         break;
    case 2:cout<<"tuesday \n";
        break;
   
   default:
    break;
   }
   */
   
   //calculator using if and else if
   /*
   int a,b;
   char op;
   cout<<"enter a:";
   cin>>a;
   cout<<"enter op:";
   cin>>op;
   cout<<"enter b:";
   cin>>b;
   
   if(op=='+'){
    cout<<"a+b="<<(a+b)<<endl;
   }
   else if(op=='-'){
    cout<<"a-b="<<(a-b)<<endl;
   }
    else if(op=='*'){
    cout<<"a*b="<<(a*b)<<endl;
    }
    else if(op=='/'){
    cout<<"a/b="<<(a/b)<<endl;
    }
   else{
    cout<<"invalid operator"<<endl;
    }
    */

   //calculator using switch
   /*
   int a,b;
   char op;
   cout<<"enter a:";
   cin>>a;
   cout<<"enter op:";
   cin>>op;
   cout<<"enter b:";
   cin>>b;
   switch (op)
   {
   case '+':  cout<<"a+b="<<(a+b)<<endl;
   break;
   case '-':   cout<<"a-b="<<(a-b)<<endl;
   break;
    case '*':  cout<<"a*b="<<(a*b)<<endl;
   break;
    case '/':  cout<<"a/b="<<(a/b)<<endl;
   break;
   default:cout<<"invalid operator"<<endl;
    break;
   }
   */

  
   


   

}